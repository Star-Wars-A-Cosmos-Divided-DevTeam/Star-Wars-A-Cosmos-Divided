Range = 350
StretchRange = 600
IdealRange = 350
Duration = 1 / (&/Simulation/PhysicsUpdatesPerSecond)
HitInterval = 1 / (&/Simulation/PhysicsUpdatesPerSecond)
TractorImpulse = 50000 * (&HitInterval) / 1 //(&<./Data/ships/terran/tractor_beam_emitter/tractor_beam_emitter.rules>/BeamCount) // Actual impulse per-ship is half; this is net total of both ships.
RampUpTime = 1
//FriendlyShipLowCollisions = false
//FriendlyShipHighCollisions = false
//NonFriendlyShipLowCollisions = false
//NonFriendlyShipHighCollisions = false
ClipRangeToTarget = true
MediaEffectsScaleOverRange = [0.6, -10]
RequireHitForRampUp = true
PenetratesStructure = false
ShieldCollisions = { Shields=false; }
InitHitLengthResetDelay = 1
//BeamMediaEffectsOffset = [39/64, 0]
//BeamMediaEffectsLengthOffset = -39/64

BeamMediaEffects
[
	&<./Data/shots/manipulator_beam/manipulator_beam_glints.rules>
	{
		Type = Beam
		IsFancy = true
		Sprite
		{
			Shader = "./Data/shots/tractor_beam/tractor_beam.shader"
			Size = [100%, 0.75]
			VertexColor = [255, 255, 255, 255]

			_extraBeginLength = 0
			_extraEndLength = 0
			_extraEndArc = 3d
			_minAlpha = .25

			Texture
			{
				File = ./Data/common_effects/particles/noise_voronoi2.png
				SampleMode = Linear
				MipLevels = max
				UMode = Wrap
				VMode = Wrap
			}
			_displacementStrength = 0.25
			_displacementSpeed = .2
			_additiveStrength = 0.4

			_noiseScaleX = 0.02
			_noiseScaleY = 2
			_noiseScrollSpeed = -2
			_noisePanSpeed = 0
			_endFadeLength = 1

			_color1 = [60, 130, 224, 255]
			_color2 = [88, 181, 224, 255]

			TargetBlendMode
			{
				SourceRgbFactor = SourceAlpha
				DestRgbFactor = InverseSourceAlpha
				RgbOperator = Add
				SourceAlphaFactor = One
				DestAlphaFactor = One
				AlphaOperator = Add
			}

			_noiseTex
			{
				File = ./Data/common_effects/particles/noise_gradient.png
				SampleMode = Linear
				MipLevels = max
				UVMode = Wrap
			}
		}
		FadeInTime = .25
		FadeOutTime = .25
		SpeedOverIntensity = [0, 0.5]
		Bucket = TractorBeam
		CaptureBackBuffer = true
		//LengthSmoothSpeed = 20
		//Start - Manipulator Beam Additions
		LengthSmoothSpeed = 80
		RotationSmoothSpeed = 80
		//End - Manipulator Beam Additions
		MinIntensity = -Infinity
	}

	: 1
	{
		IsFancy = false
		IsSimple = true
		Sprite : ^/0/Sprite
		{
			Shader = "./Data/shots/tractor_beam/tractor_beam_simple.shader"
			TargetBlendMode
			{
				SourceRgbFactor = One
				DestRgbFactor = One
				RgbOperator = Add
				SourceAlphaFactor = One
				DestAlphaFactor = One
				AlphaOperator = Add
			}
			_color1 = [60, 130, 224, 255]
			_color2 = [88, 181, 224, 255]
		}
		CaptureBackBuffer = false
	}

	: /BASE_SOUNDS/AudioExterior
	{
		Sound = "./Data/shots/tractor_beam/tractor_beam_emit.wav"
		Volume = 50%
		Speed = 100%
		RampUpTime = .25
		RampDownTime = .25

		DynamicVolume : ^/0/DynamicVolume
		{
			UseCustomShapeForDistance = true
		}
		DynamicFilter : ^/0/DynamicFilter
		{
			UseCustomShapeForDistance = true
		}
	}
]

HitNothing
{
	HitEffects
	[
		{
			Type = CollectNugget
			DeliveryDistance = 0.25
			AccelerationOverDistance = 16
			DampingOverDistance = [7, 0.25]
			OverDistance = [0.5, 8]
			DoorOpenDistance = 4
		}
		{
			Type = MoveCrew
			DeliveryDistance = 0.25
			AccelerationOverDistance = [16,32]
			DampingOverDistance = [7, 0.25]
			OverDistance = [0.5, 8]
			DoorOpenDistance = 7
		}
	]
}

HitOperational
{
	HitEffects
	[
		{
			Type = Impulse
			Impulse = -(&~/TractorImpulse) / 2
		}
		{
			Type = Feedback
			HitEffects
			[
				{
					Type = Impulse
					Impulse = (&~/TractorImpulse) / 2
				}
			]
		}
	]
}

HitStructural = &HitOperational