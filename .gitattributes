# Declare .txt files as text files that need the correct line endings
*.txt text eol=crlf
*.rules text eol=crlf

#Declare .png and .wav files as binary files that don't need line endings changed
.png binary
.wav binary

# Never let merges delete or change anything under Features/MyFeature
ships/terran/weapons/super/deathstar_modular_superlaser/** merge=ours