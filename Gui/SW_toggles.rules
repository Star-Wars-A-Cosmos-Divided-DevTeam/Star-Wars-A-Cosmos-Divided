/**
 * Defines common part toggles for armor roof types, 
 * roof normals, shield colors, thruster colors, 	
 * thruster effects, munition types & exterior looks.
 *
 * The toggles allow selecting sprite options for
 * visual variants.
 * 
 * Each toggle has a unique ID, choices with IDs, tooltips,
 * hotkeys, and sprite texture definitions.
 *
 * Can be used by parts to enable changeable visuals.
 */
PartToggles
[
	// SW Hangar Bay Clamp Off/On
	{
		ToggleID = "SW.hangar_bay_clamp_on_off"
		Style = OffAlertSwitch
		SortOrder = 6;
		Choices
		[
			// Off
			{
				ChoiceID = "SW_Hangar_Bay_Clamp_Off"
				ButtonToolTipKey = "PartToggles/SW_Hangar_Bay_Clamp_Off"
				DefaultHotkey = []
				ButtonSprite
				{
					Texture
					{
						File = "hangar/hangar_bay_clamp_off.png"
						MipLevels = 2
						SampleMode = Linear
					}
				}
			}

			// On
			{
				ChoiceID = "SW_Hangar_Bay_Clamp_On"
				ButtonToolTipKey = "PartToggles/SW_Hangar_Bay_Clamp_On"
				DefaultHotkey = []
				ButtonSprite
				{
					Texture
					{
						File = "hangar/hangar_bay_clamp_on.png"
						MipLevels = 2
						SampleMode = Linear
					}
				}
			}
		]
	}
]